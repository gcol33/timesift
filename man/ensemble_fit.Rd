% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{ensemble_fit}
\alias{ensemble_fit}
\title{Fit the combiner on the out-of-fold predictions}
\usage{
ensemble_fit(oof, y, cells, folds, spec = ensemble(), scores = NULL)
}
\arguments{
\item{oof}{Named list of \verb{[target, response]} matrices, one per candidate.}

\item{y}{The response matrix.}

\item{cells}{The scorable-cell mask from \code{\link[=scorable_cells]{scorable_cells()}}.}

\item{folds}{The fold map.}

\item{spec}{An \code{\link[=ensemble]{ensemble()}} specification.}

\item{scores}{The per-cell scores of the run, a data frame carrying \code{candidate}, \code{variable},
\code{fold}, \code{score} and \code{scorable}. Read only where \code{spec} names no metric of its own.}
}
\value{
A \code{timesift_stack}: \code{method}, the named \code{weights}, and what they were fitted on.
}
\description{
The combiner sees the out-of-fold predictions, the response, the mask of scorable cells and the
fold map, and never a model, so it cannot read anything a candidate fitted in-sample.
}
\details{
The weights are fitted to the response on those predictions, so the combined prediction scored
against the same response is scored on the data its weights were fitted to, and that score is
optimistic. \code{\link[=timesift]{timesift()}} evaluates the stack the other way: each outer fold's weights are fitted
on inner out-of-fold predictions of its training targets and applied to the outer test fold.
}
\examples{
set.seed(1)
y <- matrix(rbinom(200, 1, 0.4), nrow = 50,
            dimnames = list(sprintf("p\%02d", 1:50), paste0("sp", 1:4)))
folds <- fold_map(y, v = 5)
truth <- matrix(runif(200), nrow = 50, dimnames = dimnames(y))
oof <- list(good = 0.8 * y + 0.2 * truth, noise = truth)
ensemble_fit(oof, y, scorable_cells(y, folds), folds)

}
