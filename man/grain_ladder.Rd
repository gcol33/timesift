% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ladder.R
\name{grain_ladder}
\alias{grain_ladder}
\alias{summary.timesift_ladder}
\title{Fit at every grain and see where skill saturates}
\usage{
grain_ladder(
  x,
  y,
  learners,
  folds = NULL,
  response = "presence_absence",
  metric = NULL,
  control = train_control(),
  keep_fits = FALSE,
  verbose = TRUE
)

\method{summary}{timesift_ladder}(object, ...)
}
\arguments{
\item{x}{A \code{\link[=grain_matrix]{grain_matrix()}} result, a \code{\link[=timesift_set]{timesift_set()}}, or a named list of representations.}

\item{y}{The response for the same units.}

\item{learners}{A learner, a set of them from \code{\link[=c]{c()}}, a list, or names of registered ones. An
unnamed set is labelled by each learner's own name.}

\item{folds}{A fold map from \code{\link[=fold_map]{fold_map()}}, or any named integer vector. Built with the defaults
of \code{\link[=fold_map]{fold_map()}} when not given.}

\item{response}{Name of the registered response head.}

\item{metric}{Name of a registered metric, or a function of \verb{(y, p)}, or \code{NULL} for the
response head's own. Whichever it is, it travels with the fit and is what every later
rescoring reads; a function is reported as \verb{<function>}.}

\item{control}{\code{\link[=train_control]{train_control()}}, the training settings every neural learner of the ladder
reads. A learner carrying a control of its own overrides it on the settings that control
names.}

\item{keep_fits}{Keep every per-fold fitted model, which is what lets \code{\link[=occlusion]{occlusion()}} read a
fitted model without refitting it.}

\item{verbose}{Report each arm and each fold as it runs.}

\item{object}{A ladder.}

\item{...}{Ignored, so that \code{summary()} takes the arguments its generic declares.}
}
\value{
A data frame of one row per scored cell, of class \code{timesift_ladder}, carrying the
grain, the learner, the variable, the fold and the score. The held-out prediction of every
unit is kept in the \code{predictions} attribute, and the scorable-cell mask in \code{cells}.
}
\description{
Cross-validates every learner at every grain of a representation set, on one fold map and one
mask of scorable cells, and returns the score of each \verb{(grain, learner, variable, fold)} cell.
It is the measurement the package exists for: how much of a record a model needs, read off the
point where making the record finer stops paying.
}
\details{
Every arm sees identical splits and is restricted to identical cells, so the arms' means share a
denominator and any two of them can be compared cell by cell with \code{\link[=paired_contrast]{paired_contrast()}}.
}
\examples{
\dontshow{if (requireNamespace("glmnet", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
t <- seq(as.POSIXct("2021-09-01", tz = "UTC"), by = "hour", length.out = 24 * 200)
units <- sprintf("p\%02d", 1:60)
warmth <- rnorm(60)
d <- data.frame(
  plot = rep(units, each = length(t)), t = rep(t, length(units)),
  temp = as.numeric(vapply(warmth, function(w) w + sin(seq_along(t) / 300) + rnorm(length(t)),
                           numeric(length(t)))))
y <- matrix(rbinom(120, 1, plogis(c(warmth, -warmth))), nrow = 60,
            dimnames = list(units, c("sp1", "sp2")))
x <- grain_matrix(d, plot, t, temp, grain = c("week", "month"))
lad <- grain_ladder(x, y, elasticnet(), folds = fold_map(y, v = 3), verbose = FALSE)
summary(lad)
\dontshow{\}) # examplesIf}
}
