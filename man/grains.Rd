% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/representation.R
\name{grains}
\alias{grains}
\alias{lookbacks}
\alias{as_sift}
\title{Several representations to run the same learners across}
\usage{
grains(..., stats = "mean", year_start = "09-01")

lookbacks(..., lag = "0 days", bins = 1L, stats = "mean")

as_sift(x)
}
\arguments{
\item{...}{Grain names for \code{grains()}, lookback spans for \code{lookbacks()}, given as separate
arguments or as one vector.}

\item{stats}{Statistics computed per bin, one channel each, in the order given. See
\code{\link[=grain_matrix]{grain_matrix()}} for the seven and for what separates an extreme reading from an extreme day.}

\item{year_start}{\code{"MM-DD"} boundary of the hydrological year, used by \code{"season"} and \code{"year"}.}

\item{lag}{The gap between a target's instant and the end of its lookback.}

\item{bins}{Sub-bins the lookback is cut into, oldest first. One gives a block of features,
several give a sequence.}

\item{x}{A named list of representations, a single representation, or a character vector of
grain names.}
}
\value{
A \code{timesift_sift}: a named list of representations, labelled by each one's own label
where the list was not named.
}
\description{
The set a learner without a \verb{data =} of its own is fitted at every member of. \code{grains()} names
calendar grains, \code{lookbacks()} names lookback spans, and either takes its arguments as separate
names or as one vector.
}
\details{
\code{grains("auto")} is the set of named grains the record gives at least two bins, in the order
\code{native}, \code{halfday}, \code{day}, \code{week}, \code{month}, \code{season}, \code{year}, leaving out any grain the
requested statistics are not defined at. There is no cap on it: over three years of hourly
readings \code{native} is 26304 bins, and a caller who does not want that names the grains instead.
}
\examples{
grains("day", "week", "month")
grains(c("month", "year"), stats = c("cold_day", "mean", "warm_day"))
lookbacks("30 days", "90 days", bins = 3L)

}
