% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occlusion.R, R/report.R
\name{occlusion}
\alias{occlusion}
\alias{occlusion.default}
\alias{occlusion.timesift_ladder}
\alias{occlusion.timesift}
\title{What part of the record a fitted model reads}
\usage{
occlusion(x, ...)

\method{occlusion}{default}(x, ...)

\method{occlusion}{timesift_ladder}(
  x,
  data,
  y,
  arm,
  over = c("bin", "channel"),
  substitute = c("permute", "fold_mean", "unit_mean"),
  metric = NULL,
  permutations = 20L,
  seed = 1L,
  ...
)

\method{occlusion}{timesift}(x, candidate, over = c("bin", "channel"), ...)
}
\arguments{
\item{x}{A \code{\link[=timesift]{timesift()}} fit, or a \code{\link[=grain_ladder]{grain_ladder()}} result, in either case fitted with
\code{keep_fits = TRUE}.}

\item{...}{Passed to the method.}

\item{data}{The representation set the ladder was fitted on.}

\item{y}{The response it was fitted to.}

\item{arm}{The arm to read, as \code{"grain|learner"} or \code{"learner"}.}

\item{over}{\code{"bin"} to hold each bin back in turn, \code{"channel"} for each channel.}

\item{substitute}{What a held-back part is replaced by: \code{"permute"}, \code{"fold_mean"} or
\code{"unit_mean"}.}

\item{metric}{Name of the registered metric the rescoring is read by, or a function of
\verb{(y, p)}. Left unset it is the one the fit was scored under, so a weight is a fall in the
number \code{\link[=summary]{summary()}} reports rather than in a second one. \code{"roc_auc"} is usually the steadier
reading over many rescorings: it responds to every reordering of the units, where a maximum
over thresholds frequently does not move at all.}

\item{permutations}{Draws averaged over, for \code{substitute = "permute"}.}

\item{seed}{Random seed.}

\item{candidate}{Name of the candidate to read, for a run.}
}
\value{
A data frame of one row per held-back part and variable, carrying the mean weight over
folds and the score with and without the part.
}
\description{
Holds one bin of the record back at a time, rescores the held-out units, and records the fall in
score as that bin's weight. Nothing is refitted: the models kept by
\code{grain_ladder(keep_fits = TRUE)} are the ones read, so the profile describes the models that
produced the reported scores rather than a fresh set of them.
}
\details{
A model has to be shown something in place of a held-back bin, and what it is shown decides what
the weight means. Permuting the bin's values across units keeps the observed readings exactly
and cuts only the link between a reading and its unit. Replacing every unit by the fitting-fold
mean removes all between-unit variation while keeping the shape of the year. Replacing the bin
by each unit's own mean over the record keeps how warm a unit is and removes only that bin's
departure from it.

Read with \code{over = "channel"} the same machinery asks what each statistic of a grain carries,
holding one channel back across the whole record instead of one bin across all channels.

Reached through a \code{\link[=timesift]{timesift()}} run rather than through a ladder, the profile reads the per-fold
models the run was told to keep, so every bin is held back from a model that never saw the units
it is rescored on. The candidate is named as \code{summary()} reports it.
}
\examples{
\dontshow{if (requireNamespace("glmnet", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
t <- seq(as.POSIXct("2021-09-01", tz = "UTC"), by = "hour", length.out = 24 * 120)
units <- sprintf("p\%02d", 1:40)
warmth <- rnorm(40)
d <- data.frame(
  plot = rep(units, each = length(t)), t = rep(t, length(units)),
  temp = as.numeric(vapply(warmth, function(w) w + sin(seq_along(t) / 300) + rnorm(length(t)),
                           numeric(length(t)))))
y <- matrix(rbinom(80, 1, plogis(c(warmth, -warmth))), nrow = 40,
            dimnames = list(units, c("sp1", "sp2")))
x <- grain_matrix(d, plot, t, temp, grain = "month")
lad <- grain_ladder(x, y, elasticnet(), folds = fold_map(y, v = 3),
                     keep_fits = TRUE, verbose = FALSE)
head(occlusion(lad, x, y, "month|elasticnet", permutations = 3))
\dontshow{\}) # examplesIf}
}
