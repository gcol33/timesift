% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timesift.R
\name{predict.timesift}
\alias{predict.timesift}
\title{Predict from a fitted timesift}
\usage{
\method{predict}{timesift}(object, targets, series = NULL, candidate = "ensemble", ...)
}
\arguments{
\item{object}{A \code{\link[=timesift]{timesift()}} fit.}

\item{targets}{A data frame of targets, carrying the identifier, the anchor and the static
columns the fit was given.}

\item{series}{The long table of readings for those targets, or \code{NULL} for a targets-only fit.}

\item{candidate}{\code{"ensemble"}, the stack refitted on every target; \code{"selected"}, the candidate
the rule chose on every target (\code{object$choice}); or the name of one candidate.}

\item{...}{Ignored.}
}
\value{
A \verb{[target, response]} matrix of predictions, named by target and in the order the fit
carries its own targets: sorted by identifier, or the targets' own order where \code{target_time}
anchors them.
}
\description{
Rebuilds every member's representation for the new targets from the settings its own arm was
built with, predicts with the model refitted on all targets, and combines them where the
ensemble is asked for.
}
