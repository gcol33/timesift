% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_response}
\alias{register_response}
\alias{responses}
\title{Register a response head}
\usage{
register_response(name, spec, overwrite = FALSE)

responses()
}
\arguments{
\item{name}{Name the response is asked for by.}

\item{spec}{A list with elements \code{prepare(y)}, returning the numeric matrix a learner is fitted
on; \code{activation}, the name of the output transform (\code{"sigmoid"} or \code{"identity"}); \code{loss}, the
name of the training objective (\code{"binary_cross_entropy"} or \code{"squared_error"}); \code{metric}, the
default metric name; and \code{cells(y, folds)}, returning the mask of scorable cells. Every
learner that ships reads \code{loss} and \code{activation} from here: the encoders train under the loss
and predict through the activation, and the learners fitting one model per response take the
family the loss names, logistic or Gaussian. The combiner minimises the same loss. An
optional \code{weights(y)} returns a \verb{[unit, variable]} matrix of case weights every learner fits
under; the shipped head's is \code{\link[=positive_weights]{positive_weights()}}, and a head without one fits unweighted.}

\item{overwrite}{Replace an existing registration.}
}
\value{
The registered specification, invisibly.
}
\description{
A response head says what the values being predicted are, how they reach a learner, and which
cells of the (variable, fold) grid a score is defined on. Presence-absence with a joint
multi-label head is what ships; an abundance or phenology response is a registration rather than
a second fitting path.
}
\examples{
responses()

}
