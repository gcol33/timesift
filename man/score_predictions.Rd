% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ladder.R
\name{score_predictions}
\alias{score_predictions}
\title{Score held-out predictions on the cells the mask allows}
\usage{
score_predictions(y, p, folds, cells = NULL, metric = "tss")
}
\arguments{
\item{y}{The response, as a matrix of units by variables.}

\item{p}{Held-out predictions for the same units and variables.}

\item{folds}{A \code{\link[=fold_map]{fold_map()}} result, or one fold per unit.}

\item{cells}{A \code{\link[=scorable_cells]{scorable_cells()}} mask. Computed from \code{y} and \code{folds} when left unset.}

\item{metric}{Name of a registered metric to read the cells by, or a function of \verb{(y, p)}.}
}
\value{
A data frame of one row per variable and fold, carrying the score and whether the cell
was scorable.
}
\description{
The scoring every arm of a ladder and every candidate of a run goes through, reachable on its
own for a prediction matrix that came from somewhere else: a combination of arms, a model fitted
outside the package, predictions read back from a file.
}
\details{
A cell is one response in one fold. Scoring only the cells the mask allows is what keeps two
arms comparable, so the mask is computed from the response and the fold map alone and never from
a model.
}
\examples{
set.seed(1)
y <- matrix(rbinom(120, 1, 0.4), nrow = 30,
            dimnames = list(sprintf("p\%02d", 1:30), paste0("sp", 1:4)))
p <- matrix(runif(120), nrow = 30, dimnames = dimnames(y))
head(score_predictions(y, p, fold_map(y, v = 3)))

}
